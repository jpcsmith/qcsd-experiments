---
- name: Push changed experiment files from the localhost to the remote.
  hosts:
    - wg_clients
    - ml_host
    - zapdos
  gather_facts: false
  tasks:
    - import_tasks: tasks/sync_experiment.yml
